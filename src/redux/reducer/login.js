import actionTypes from '../actions/actionTypes';

const INITIAL_STATE = {
  nome,
  email,
};

export default loginReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  case '':
    return state;
  default:
    return state;
  }
};
